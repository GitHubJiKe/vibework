# vibework · 踩坑记录

> 开发过程中遇到并解决的真实问题，按「现象 → 原因 → 解决 → 教训」整理。
> 希望后来者少走弯路。

## 1. CGEvent 坐标系统：不要翻转 Y 轴

- **现象**：手机上手指往上滑，Mac 鼠标往下走；方向完全相反。
- **原因**：误以为 ScreenCaptureKit 的窗口 frame（左上原点）与 CGEvent 鼠标坐标（左下原点）不同，做了 Y 翻转。
- **解决**：去掉翻转。`SCWindow.frame` / `SCDisplay.frame` 与 CGEvent 鼠标坐标**都是全局显示坐标（左上原点）**，直接相加即可。
- **教训**：两个 API 的坐标系是否一致，先查文档再写映射，不要凭直觉假设。

## 2. 滚轮事件只发给「鼠标悬停位置」的窗口

- **现象**：滚动时灵时不灵，只有先在电脑上手动滚一下之后才生效。
- **原因**：macOS 把滚轮事件路由给鼠标当前悬停的窗口。代码先移动鼠标、立刻发滚轮，两个事件异步投递，滚轮经常落在鼠标移动前的位置（别的窗口/不可滚动区域）。
- **解决**：移动鼠标后**等 40ms 让鼠标落位**，再发滚轮事件。
- **教训**：注入「鼠标移动 + 点击/滚轮」组合事件时，必须考虑事件投递的时序，不能假设立即生效。

## 3. 滚动锚点不能跟随「用户上次点击的位置」

- **现象**：控制 Codex 时滚动完全无效，但 Cursor 正常。
- **原因**：滚动锚点 = 用户最近一次在手机上点过的位置（lastPointer）。日志显示锚点落在 Codex 窗口左侧栏/空白区，那里不可滚动，滚轮事件全部无效。
- **解决**：滚动锚点**固定为窗口中央的聊天/主内容区**，不跟随用户点击位置。
- **教训**：远程控制的滚动要锚定在「可滚动的主内容区」，不能假设用户点过的地方都能滚；排查这类问题，打日志看滚轮到底发到了哪个坐标。

## 4. 输入框聚焦是「应用相关」的，不能一刀切

- **现象**：文字发不进 Codex 输入框；后来给 Cursor 也加上「点击输入框聚焦」后，**Cursor 也失效了**。
- **原因**：
  - Cursor（Electron）被激活后焦点天然在输入框，直接注入文字即可；
  - Codex 被激活后焦点不在输入框，必须先用鼠标点击输入框拿到焦点；
  - 给 Cursor 也做点击聚焦，反而把焦点从输入框抢走了。
- **解决**：按 bundle id 差异化——**仅对 Codex**（`com.openai.codex`）发送前自动点击底部输入框聚焦，等待 0.4 秒再注入文字；Cursor 保持「激活即输入」。
- **教训**：文字注入机制是通用的，但「焦点在不在输入框」因 App 而异。聚焦策略按 App 配置，不能对所有应用统一处理。

## 5. 多应用切换：`switch_ok` 必须在切流完成后返回

- **现象**：切换应用后立刻操作，指令落到旧窗口（文字进错 App、滚动无效）。
- **原因**：`switch_ok` 在切流完成前就返回，客户端 UI 已高亮新应用，但服务端还在停旧流/启新流。
- **解决**：切流成功后由 `notifySwitch` 广播 `switch_ok`，客户端收到回执才更新高亮。
- **教训**：异步状态切换（切流）与 UI 回执必须同步，先完成、再通知。

## 6. Swift 多行字符串吞换行 → HTTP 头损坏

- **现象**：浏览器打开页面白屏，手机 Safari「无法解析响应」。
- **原因**：Swift 多行字符串会吞掉结尾换行，HTTP 响应头里的空行变成单个 `\r`，浏览器无法识别头部结束。
- **解决**：HTTP 头一律显式拼接 `\r\n`，不依赖多行字符串。
- **教训**：手写 HTTP 协议时，头部结束符要显式控制。

## 7. HTTP 响应发完立即 `cancel()` 会截断

- **现象**：页面内容不完整。
- **原因**：`conn.cancel()` 在发送完成后立即调用，尾部数据被丢弃。
- **解决**：发送完成后保持连接，等客户端关闭再释放。
- **教训**：Network 框架的发送是异步的，取消连接要在确认数据发出后。

## 8. 屏幕静止时 ScreenCaptureKit 不产帧

- **现象**：启动后一直「等待画面帧」，画面完全静止时手机看不到屏幕。
- **原因**：屏幕内容无变化时 SCStream 不产生新帧。
- **解决**：每秒 `updateConfiguration` 微调帧率（nudge）强制出一帧 + 每秒重发最后一帧兜底。
- **教训**：基于 SCK 的推流必须处理静止场景，否则用户会看到黑屏/卡住。

## 9. DeepSeek API：模型名与 Key 持久化

- **现象**：AI 润色调不通，报「API Key 无效」；换新 Key 重启后又失效。
- **原因**：
  - `deepseek-chat` / `deepseek-reasoner` 已于 2026-07-24 弃用，现行模型名为 `deepseek-v4-flash` / `deepseek-v4-pro`；
  - 页面 ⚙ 设置的 Key 只存在内存，重启即丢失，回退到启动参数的旧 Key。
- **解决**：模型名改用 `deepseek-v4-flash`；Key 持久化到 `~/.vibework/deepseek.key`（权限 600），重启自动加载。
- **教训**：外部 API 的模型名会变，要查官方文档；「内存态配置」对常驻服务是坑，能持久化就持久化。

## 10. 多窗口捕获：优先「按需切换」而非「同时多流」

- **现象**：多窗口同时捕获时出现「捕获意外停止：流播放期间，应用程序连接中断而导致失败」。
- **解决**：改为**按需切换**——同一时刻只启动一个 SCStream，切换应用时先 `stopCapture()` 旧流再 `startCapture()` 新窗口流，稳定且省资源。
- **教训**：多路屏幕采集对资源/权限敏感，能用单流轮换就避免同时多流；若多流是硬需求，务必在真实环境验证并发上限。
